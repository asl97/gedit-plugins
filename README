General Information
===================

This is a fork from version 3.22.0 of gedit-plugins.

gedit-plugins are a set of plugins for gedit.

This fork exists cause upstream is taking too long and plugins are broken

Installation
============

For the moment, you have to ./autogen.sh and ./configure with --prefix=same prefix as you
configured gedit with.

